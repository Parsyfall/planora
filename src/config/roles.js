export const ROLES = Object.freeze({
  USER: 'USER',
  ORGANIZER: 'ORGANIZER'
});

export const INVITATION_STATUS = Object.freeze({
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  DECLINED: 'declined'
});
